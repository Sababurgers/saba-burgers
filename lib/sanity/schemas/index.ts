import { categorySchema } from "./category";
import { productSchema } from "./product";
import { siteSettingsSchema } from "./siteSettings";

export const schemaTypes = [siteSettingsSchema, categorySchema, productSchema];
